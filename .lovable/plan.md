

## Plan: Move Prayer & Islamic Calendar Below Daily Check-in

Move `DashboardPrayerWidget` and `DashboardIslamicCalendarWidget` from lines 130-131 to right after the check-in indicator (after line 101), before the Workload widget.

Single reorder in `src/pages/EmployeeHome.tsx`, no logic changes.

